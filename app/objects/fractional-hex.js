import EmberObject, { computed } from '@ember/object';
import {assert} from '@ember/debug';
import FractionalHex from './fractional-hex';

export default EmberObject.extend({
  name: 'FractionalHex',
});
